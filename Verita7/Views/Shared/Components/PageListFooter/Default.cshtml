@using Verita.Domain.Entities;
@model PageViewModel
 
@if (Model.Pages != null )
{
   
    @foreach (var pageModel in Model.Pages)
    {
        <li>
            <a href="/Page?pageId=@pageModel.Id">@pageModel.Name</a>
        </li> 
    }
    @if (Model.Pages.FirstOrDefault()!.PageCategory == Verita.Common.Enums.PageCategory.MerakEttikleriniz)
    {
        <li>
            <a href="/bunlari-biliyor-musunuz/">Bunları Biliyor Musunuz?</a>
        </li> 
    }
}