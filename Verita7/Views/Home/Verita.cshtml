@{
    ViewBag.BodyClass = "shop-header-shadow shop-home";
}
<link href="/Themes/Default/Content/css/pages/page.min.css?v=2020100520" rel="stylesheet" type="text/css" />
<main class="catalog-page mt-40">
    <div class="filter-box categoryNav">
        <ul>

            <li class="active last">
                <a href="/urunlerimiz/">
                    Tüm Ürünler
                </a>
            </li>

            <li class="inactive">
                <a href="/egzotik-meyveler/">
                    Egzotik Meyveler
                </a>
            </li>

            <li class="inactive">
                <a href="/orman-meyveleri/">
                    Orman Meyveleri
                </a>
            </li>

            <li class="inactive">
                <a href="/yemeye-hazir/">
                    Yemeye Hazır
                </a>
            </li>

            <li class="inactive">
                <a href="/nadir-bulunan-meyveler/">
                    Verita Raw Juice
                </a>
            </li>

            <li class="inactive">
                <a href="/verita-fresh-cut/">
                    Verita Fresh Cut
                </a>
            </li>

            <li class="inactive">
                <a href="/sebzeler-ve-kok-bitkiler/">
                    Sebzeler ve Kök Bitkiler
                </a>
            </li>
        </ul>
    </div>
    <div class="container">
        <div class="page-content pe-0">
            <div class="heading-title mb-5">
                <h4>Verita</h4>

            </div>
            <div class="topic-content">
                <div class="section-title">
                    <h2><small>Verita</small></h2>
                </div>
                <div class="umb-grid">
                    <div class="grid-section">
                        <div>
                            <div class="single-column">
                                <div class="row clearfix">
                                    <div class="col-md-12 column">
                                        <div>

                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="container map-container">
            <div class="row">
                <div class="col-md-12">
                    <div class="on-map-table-container single-column">
                        <div class="on-map-table">
                            <h2>Hangi &#220;r&#252;nler?</h2>
                            <ul class="" id="urunListesi" style="display:none"></ul>
                        </div>
                        <div class="on-map-table text-center text-left">
                            <h2>Nereden?</h2>
                            <ul class="" id="location"></ul>
                        </div>
                        <div class="on-map-table" style="right:0">
                            <h2>Nereye?</h2>
                            <ul class="" id="destinations" style="display:none"></ul>
                        </div>
                    </div>
                </div>
                <div class="col-md-offset-1 col-md-10">
                    <div id="chartdiv" class="chartdiv-dunya"></div>
                </div>
            </div>
        </div>
    </div>
</main>

@section links{
    <link href="/Themes/Default/Content/css/ideal.css" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/plugins/toastr.min.css?v=2020100520" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/layout/footer.min.css?v=2020100520" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/layout/shop/catalog.css?v=2020100520" rel="stylesheet" type="text/css" />
    <script src="/Themes/Default/Content/js/bootstrap/bootstrap.bundle.min.js?v=2020100520" type="text/javascript"></script>
    <script src="/Themes/Default/Content/plugins/jquery-inline-svg/jquery-inline-svg.min.js?v=2020100520" type="text/javascript"></script>
    <script src="/Themes/Default/Content/plugins/swiperjs/swiper-bundle.min.js?v=2020100520" type="text/javascript"></script>
    <script src="/lib/jquery-validate/jquery.validate-v1.17.0/jquery.validate.min.js?v=2020100520" type="text/javascript"></script>
    <script src="/lib/jquery-validate/jquery.validate.unobtrusive-v3.2.10/jquery.validate.unobtrusive.min.js?v=2020100520" type="text/javascript"></script>
    <script src="/lib/jquery-migrate/jquery-migrate-3.0.1.min.js?v=2020100520" type="text/javascript"></script>
    <script src="/lib/responsive-toolkit/responsive-toolkit.min.js?v=2020100520" type="text/javascript"></script>
    <link href="/Themes/Default/Content/css/pages/page.min.css?v=2020100520" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/plugins/toastr.min.css?v=2020100520" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/layout/component/heading-title.min.css?v=2020100520" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/layout/shop/catalog.css?v=2020100520" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/layout/shop/product-showcase-home.min.css?v=2020100520" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/layout/component/product-item.min.css?v=2020100520" rel="stylesheet" type="text/css" />
    <link href="/Themes/Default/Content/css/layout/footer.min.css?v=2020100520" rel="stylesheet" type="text/css" />
    <script src="/Themes/Default/Content/js/main.js?v=2020100520" type="text/javascript"></script>
    <script src="/js/public.common.min.js?v=2020100520" type="text/javascript"></script>
    <script src="/js/public.ajaxcart.js?v=2020100520" type="text/javascript"></script>
    <script src="/lib/toastr/2.1.4/toastr.min.js?v=2020100520" type="text/javascript"></script>

    <script src="https://www.amcharts.com/lib/4/core.js"></script>
    <script src="https://www.amcharts.com/lib/4/maps.js"></script>
    <script src="https://www.amcharts.com/lib/4/geodata/worldLow.js"></script>
    <script src="https://www.amcharts.com/lib/4/themes/animated.js"></script>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <script src="/Themes/Default/Content/js/scripts_ctx.js" type="text/javascript"></script>
    <script src="/Themes/Default/Content/js/map.js" type="text/javascript"></script>


 

}

@section scripts{
    <script>
        AjaxCart.init('.header-links .qty-cart', '.header-links .qty-wishlist', '.flyout-cart');
    </script>
    <script>
        var $mobileAccount = $('.header-mobile .account-dropdown');

        $mobileAccount.on('click', function () {
            if ($mobileAccount.find('.navbar-right-item-title').hasClass('is-authenticated')) {
                window.location.pathname = '/hesabim/bilgilerim/';
            }

            if ($mobileAccount.find('.navbar-right-item-title').hasClass('no-authenticated')) {
                window.location.pathname = '/kullanici-giris/';
            }

        })
    </script>

    <script>
        var originCtx = [];
        var destinations = [];
        var urunler = [];

        var obj = {};


        obj.title = "Germany";
        obj.id = "Germany".toLowerCase();
        if (destinations.find((element) => { return element.title == "Germany" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Germany" }), 1)
        }
        if (data.find((element) => { return element.name == "Germany" }) != undefined) {
            originCtx.push({
                "id": "Germany".toLowerCase(),
                "title": "Germany",
                "gorunecekIsim": "Almanya",
                "destinations": ["Türkiye"],
                "latitude": data.find((element) => { return element.name == "Germany" }).latitude,
                "longitude": data.find((element) => { return element.name == "Germany" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Germany  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Elma', 'Kavun', 'Karpuz', 'Domates', 'Salatalık', 'Patlıcan'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Turkey";
        obj.id = "Turkey".toLowerCase();
        if (destinations.find((element) => { return element.title == "Turkey" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Turkey" }), 1)
        }
        if (data.find((element) => { return element.name == "Turkey" }) != undefined) {
            originCtx.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "destinations": ["Malezya", "Hindistan", "Mauritius", "Türkiye", "Maldivler"],
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Turkey  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Malaysia" }) == null && originCtx.find((element) => { return element.title == "Malaysia" }) == null) {
            destinations.push({
                "id": "Malaysia".toLowerCase(),
                "title": "Malaysia",
                "gorunecekIsim": "Malezya",
                "latitude": data.find((element) => { return element.name == "Malaysia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Malaysia" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "India" }) == null && originCtx.find((element) => { return element.title == "India" }) == null) {
            destinations.push({
                "id": "India".toLowerCase(),
                "title": "India",
                "gorunecekIsim": "Hindistan",
                "latitude": data.find((element) => { return element.name == "India" }).latitude,
                "longitude": data.find((element) => { return element.name == "India" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Mauritius" }) == null && originCtx.find((element) => { return element.title == "Mauritius" }) == null) {
            destinations.push({
                "id": "Mauritius".toLowerCase(),
                "title": "Mauritius",
                "gorunecekIsim": "Mauritius",
                "latitude": data.find((element) => { return element.name == "Mauritius" }).latitude,
                "longitude": data.find((element) => { return element.name == "Mauritius" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Maldives" }) == null && originCtx.find((element) => { return element.title == "Maldives" }) == null) {
            destinations.push({
                "id": "Maldives".toLowerCase(),
                "title": "Maldives",
                "gorunecekIsim": "Maldivler",
                "latitude": data.find((element) => { return element.name == "Maldives" }).latitude,
                "longitude": data.find((element) => { return element.name == "Maldives" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Kivi, Avokado', 'Kumkuat, Altın Çilek', 'Pepino, Böğürtlen', 'Frenk Üzümü, ', 'Yaban Mersini', 'Ahududu, Kuşkonmaz', 'Elma, Armut', 'Üzüm, Nar', 'Çilek, Kaktüs İnciri', 'Kiraz, İncir', 'Erik, Kayısı', 'Şeftali, Nektarin', 'Pitahaya, Ayva', 'Mandalina, Portakal', 'Greyfurt, Limon'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Mexico";
        obj.id = "Mexico".toLowerCase();
        if (destinations.find((element) => { return element.title == "Mexico" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Mexico" }), 1)
        }
        if (data.find((element) => { return element.name == "Mexico" }) != undefined) {
            originCtx.push({
                "id": "Mexico".toLowerCase(),
                "title": "Mexico",
                "gorunecekIsim": "Meksika",
                "destinations": ["Hollanda", "Türkiye"],
                "latitude": data.find((element) => { return element.name == "Mexico" }).latitude,
                "longitude": data.find((element) => { return element.name == "Mexico" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Mexico  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Böğürtlen', 'Böğürtlen (By Air)', 'Ahududu', 'Ahududu (By Air)', 'Kuşkonmaz', 'Kaktüs İnciri', 'Lime', 'Kavun', 'Liçi', 'Tomatillo'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Honduras";
        obj.id = "Honduras".toLowerCase();
        if (destinations.find((element) => { return element.title == "Honduras" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Honduras" }), 1)
        }
        if (data.find((element) => { return element.name == "Honduras" }) != undefined) {
            originCtx.push({
                "id": "Honduras".toLowerCase(),
                "title": "Honduras",
                "gorunecekIsim": "Honduras",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Honduras" }).latitude,
                "longitude": data.find((element) => { return element.name == "Honduras" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Honduras  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Tatlı Patates', 'Kavun'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Costa Rica";
        obj.id = "Costa Rica".toLowerCase();
        if (destinations.find((element) => { return element.title == "Costa Rica" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Costa Rica" }), 1)
        }
        if (data.find((element) => { return element.name == "Costa Rica" }) != undefined) {
            originCtx.push({
                "id": "Costa Rica".toLowerCase(),
                "title": "Costa Rica",
                "gorunecekIsim": "Kosta Rika",
                "destinations": ["Hollanda", "Türkiye", "İngiltere", "İtalya", "Rusya", "Litvanya", "Beyaz Rusya", "Gürcistan", "Ermenistan", "Güney Kore"],
                "latitude": data.find((element) => { return element.name == "Costa Rica" }).latitude,
                "longitude": data.find((element) => { return element.name == "Costa Rica" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Costa Rica  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "United Kingdom" }) == null && originCtx.find((element) => { return element.title == "United Kingdom" }) == null) {
            destinations.push({
                "id": "United Kingdom".toLowerCase(),
                "title": "United Kingdom",
                "gorunecekIsim": "İngiltere",
                "latitude": data.find((element) => { return element.name == "United Kingdom" }).latitude,
                "longitude": data.find((element) => { return element.name == "United Kingdom" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Italy" }) == null && originCtx.find((element) => { return element.title == "Italy" }) == null) {
            destinations.push({
                "id": "Italy".toLowerCase(),
                "title": "Italy",
                "gorunecekIsim": "İtalya",
                "latitude": data.find((element) => { return element.name == "Italy" }).latitude,
                "longitude": data.find((element) => { return element.name == "Italy" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Russia" }) == null && originCtx.find((element) => { return element.title == "Russia" }) == null) {
            destinations.push({
                "id": "Russia".toLowerCase(),
                "title": "Russia",
                "gorunecekIsim": "Rusya",
                "latitude": data.find((element) => { return element.name == "Russia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Russia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Lithuania" }) == null && originCtx.find((element) => { return element.title == "Lithuania" }) == null) {
            destinations.push({
                "id": "Lithuania".toLowerCase(),
                "title": "Lithuania",
                "gorunecekIsim": "Litvanya",
                "latitude": data.find((element) => { return element.name == "Lithuania" }).latitude,
                "longitude": data.find((element) => { return element.name == "Lithuania" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Belarus" }) == null && originCtx.find((element) => { return element.title == "Belarus" }) == null) {
            destinations.push({
                "id": "Belarus".toLowerCase(),
                "title": "Belarus",
                "gorunecekIsim": "Beyaz Rusya",
                "latitude": data.find((element) => { return element.name == "Belarus" }).latitude,
                "longitude": data.find((element) => { return element.name == "Belarus" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Georgia" }) == null && originCtx.find((element) => { return element.title == "Georgia" }) == null) {
            destinations.push({
                "id": "Georgia".toLowerCase(),
                "title": "Georgia",
                "gorunecekIsim": "Gürcistan",
                "latitude": data.find((element) => { return element.name == "Georgia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Georgia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Armenia" }) == null && originCtx.find((element) => { return element.title == "Armenia" }) == null) {
            destinations.push({
                "id": "Armenia".toLowerCase(),
                "title": "Armenia",
                "gorunecekIsim": "Ermenistan",
                "latitude": data.find((element) => { return element.name == "Armenia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Armenia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "South Korea" }) == null && originCtx.find((element) => { return element.title == "South Korea" }) == null) {
            destinations.push({
                "id": "South Korea".toLowerCase(),
                "title": "South Korea",
                "gorunecekIsim": "Güney Kore",
                "latitude": data.find((element) => { return element.name == "South Korea" }).latitude,
                "longitude": data.find((element) => { return element.name == "South Korea" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        obj.urunler = ['Ananas', 'Kavun', 'Eddo', 'Karpuz', 'Kasava', 'Ananas (By Air)'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Ecuador";
        obj.id = "Ecuador".toLowerCase();
        if (destinations.find((element) => { return element.title == "Ecuador" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Ecuador" }), 1)
        }
        if (data.find((element) => { return element.name == "Ecuador" }) != undefined) {
            originCtx.push({
                "id": "Ecuador".toLowerCase(),
                "title": "Ecuador",
                "gorunecekIsim": "Ekvador",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Ecuador" }).latitude,
                "longitude": data.find((element) => { return element.name == "Ecuador" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Ecuador  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Papaya', 'Muz', 'Mango', 'Sarı Pitahaya', 'Pepino', 'Granadilla', 'Guanabana'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Peru";
        obj.id = "Peru".toLowerCase();
        if (destinations.find((element) => { return element.title == "Peru" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Peru" }), 1)
        }
        if (data.find((element) => { return element.name == "Peru" }) != undefined) {
            originCtx.push({
                "id": "Peru".toLowerCase(),
                "title": "Peru",
                "gorunecekIsim": "Peru",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "Peru" }).latitude,
                "longitude": data.find((element) => { return element.name == "Peru" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Peru  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Avokado', 'Mango', 'Üzüm', 'Nar', 'Zerdeçal', 'Kuşkonmaz', 'Kuşkonmaz (By Air)', 'Yaban mersini', 'Yaban Mersini (By Air)'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Chile";
        obj.id = "Chile".toLowerCase();
        if (destinations.find((element) => { return element.title == "Chile" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Chile" }), 1)
        }
        if (data.find((element) => { return element.name == "Chile" }) != undefined) {
            originCtx.push({
                "id": "Chile".toLowerCase(),
                "title": "Chile",
                "gorunecekIsim": "Şili",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "Chile" }).latitude,
                "longitude": data.find((element) => { return element.name == "Chile" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Chile  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Kivi', 'Yaban mersini', 'Yaban Mersini (By Air)', 'Frenk üzümü', 'Elma', 'Armut', 'Üzüm', 'Nar', 'Ceviz', 'Avokado', 'Mango', 'Erik', 'Kiraz', 'Kiraz (By Air)', 'Nektarin', 'Şeftali'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Panama";
        obj.id = "Panama".toLowerCase();
        if (destinations.find((element) => { return element.title == "Panama" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Panama" }), 1)
        }
        if (data.find((element) => { return element.name == "Panama" }) != undefined) {
            originCtx.push({
                "id": "Panama".toLowerCase(),
                "title": "Panama",
                "gorunecekIsim": "Panama",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Panama" }).latitude,
                "longitude": data.find((element) => { return element.name == "Panama" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Panama  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Ananas'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Dominican Republic";
        obj.id = "Dominican Republic".toLowerCase();
        if (destinations.find((element) => { return element.title == "Dominican Republic" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Dominican Republic" }), 1)
        }
        if (data.find((element) => { return element.name == "Dominican Republic" }) != undefined) {
            originCtx.push({
                "id": "Dominican Republic".toLowerCase(),
                "title": "Dominican Republic",
                "gorunecekIsim": "Dominik",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Dominican Republic" }).latitude,
                "longitude": data.find((element) => { return element.name == "Dominican Republic" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Dominican Republic  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Mango', 'Hindistan Cevizi'];
        urunler.push(obj);


        var obj = {};


        obj.title = "United States";
        obj.id = "United States".toLowerCase();
        if (destinations.find((element) => { return element.title == "United States" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "United States" }), 1)
        }
        if (data.find((element) => { return element.name == "United States" }) != undefined) {
            originCtx.push({
                "id": "United States".toLowerCase(),
                "title": "United States",
                "gorunecekIsim": "ABD",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "United States" }).latitude,
                "longitude": data.find((element) => { return element.name == "United States" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("United States  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Tatlı Patates'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Spain";
        obj.id = "Spain".toLowerCase();
        if (destinations.find((element) => { return element.title == "Spain" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Spain" }), 1)
        }
        if (data.find((element) => { return element.name == "Spain" }) != undefined) {
            originCtx.push({
                "id": "Spain".toLowerCase(),
                "title": "Spain",
                "gorunecekIsim": "İspanya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Spain" }).latitude,
                "longitude": data.find((element) => { return element.name == "Spain" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Spain  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Kumkuat', 'Böğürtlen', 'Yaban Mersini', 'Ahududu', 'Iceberg Marul', 'Avokado', 'Çerimoya', 'Kaki', 'Mango', 'Tomatillo', 'Tatlı Patates'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Portugal";
        obj.id = "Portugal".toLowerCase();
        if (destinations.find((element) => { return element.title == "Portugal" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Portugal" }), 1)
        }
        if (data.find((element) => { return element.name == "Portugal" }) != undefined) {
            originCtx.push({
                "id": "Portugal".toLowerCase(),
                "title": "Portugal",
                "gorunecekIsim": "Portekiz",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "Portugal" }).latitude,
                "longitude": data.find((element) => { return element.name == "Portugal" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Portugal  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Böğürtlen', 'Frenk Üzümü', 'Yaban Mersini', 'Ahududu', 'Ahududu (By Air)'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Colombia";
        obj.id = "Colombia".toLowerCase();
        if (destinations.find((element) => { return element.title == "Colombia" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Colombia" }), 1)
        }
        if (data.find((element) => { return element.name == "Colombia" }) != undefined) {
            originCtx.push({
                "id": "Colombia".toLowerCase(),
                "title": "Colombia",
                "gorunecekIsim": "Kolombiya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Colombia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Colombia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Colombia  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Pitahaya', 'Altın Çilek', 'Granadilla', 'Mangosten', 'Çarkıfelek', 'Guanabana', 'Tamarillo', 'Avokado', 'Maracuya'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Brazil";
        obj.id = "Brazil".toLowerCase();
        if (destinations.find((element) => { return element.title == "Brazil" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Brazil" }), 1)
        }
        if (data.find((element) => { return element.name == "Brazil" }) != undefined) {
            originCtx.push({
                "id": "Brazil".toLowerCase(),
                "title": "Brazil",
                "gorunecekIsim": "Brezilya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Brazil" }).latitude,
                "longitude": data.find((element) => { return element.name == "Brazil" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Brazil  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Mango', 'Lime', 'Kavun', 'Taze zencefil', 'Yam', 'Karpuz', 'Papaya', 'Rambutan', 'Liçi'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Uruguay";
        obj.id = "Uruguay".toLowerCase();
        if (destinations.find((element) => { return element.title == "Uruguay" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Uruguay" }), 1)
        }
        if (data.find((element) => { return element.name == "Uruguay" }) != undefined) {
            originCtx.push({
                "id": "Uruguay".toLowerCase(),
                "title": "Uruguay",
                "gorunecekIsim": "Uruguay",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "Uruguay" }).latitude,
                "longitude": data.find((element) => { return element.name == "Uruguay" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Uruguay  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Yaban Mersini', 'Yaban Mersini (By Air)', 'Armut'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Argentina";
        obj.id = "Argentina".toLowerCase();
        if (destinations.find((element) => { return element.title == "Argentina" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Argentina" }), 1)
        }
        if (data.find((element) => { return element.name == "Argentina" }) != undefined) {
            originCtx.push({
                "id": "Argentina".toLowerCase(),
                "title": "Argentina",
                "gorunecekIsim": "Arjantin",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "Argentina" }).latitude,
                "longitude": data.find((element) => { return element.name == "Argentina" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Argentina  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Yaban Mersini', 'Armut', 'Limon', 'Kiraz', 'Yaban Mersini (By Air)', 'Kiraz (By Air)'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Netherlands";
        obj.id = "Netherlands".toLowerCase();
        if (destinations.find((element) => { return element.title == "Netherlands" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Netherlands" }), 1)
        }
        if (data.find((element) => { return element.name == "Netherlands" }) != undefined) {
            originCtx.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "destinations": ["Türkiye", "Rusya", "Beyaz Rusya", "Ukrayna", "Gürcistan", "Türkmenistan", "Ermenistan", "Azerbaycan"],
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Netherlands  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Russia" }) == null && originCtx.find((element) => { return element.title == "Russia" }) == null) {
            destinations.push({
                "id": "Russia".toLowerCase(),
                "title": "Russia",
                "gorunecekIsim": "Rusya",
                "latitude": data.find((element) => { return element.name == "Russia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Russia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Belarus" }) == null && originCtx.find((element) => { return element.title == "Belarus" }) == null) {
            destinations.push({
                "id": "Belarus".toLowerCase(),
                "title": "Belarus",
                "gorunecekIsim": "Beyaz Rusya",
                "latitude": data.find((element) => { return element.name == "Belarus" }).latitude,
                "longitude": data.find((element) => { return element.name == "Belarus" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Ukraine" }) == null && originCtx.find((element) => { return element.title == "Ukraine" }) == null) {
            destinations.push({
                "id": "Ukraine".toLowerCase(),
                "title": "Ukraine",
                "gorunecekIsim": "Ukrayna",
                "latitude": data.find((element) => { return element.name == "Ukraine" }).latitude,
                "longitude": data.find((element) => { return element.name == "Ukraine" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Georgia" }) == null && originCtx.find((element) => { return element.title == "Georgia" }) == null) {
            destinations.push({
                "id": "Georgia".toLowerCase(),
                "title": "Georgia",
                "gorunecekIsim": "Gürcistan",
                "latitude": data.find((element) => { return element.name == "Georgia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Georgia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Turkmenistan" }) == null && originCtx.find((element) => { return element.title == "Turkmenistan" }) == null) {
            destinations.push({
                "id": "Turkmenistan".toLowerCase(),
                "title": "Turkmenistan",
                "gorunecekIsim": "Türkmenistan",
                "latitude": data.find((element) => { return element.name == "Turkmenistan" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkmenistan" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Armenia" }) == null && originCtx.find((element) => { return element.title == "Armenia" }) == null) {
            destinations.push({
                "id": "Armenia".toLowerCase(),
                "title": "Armenia",
                "gorunecekIsim": "Ermenistan",
                "latitude": data.find((element) => { return element.name == "Armenia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Armenia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Azerbaijan" }) == null && originCtx.find((element) => { return element.title == "Azerbaijan" }) == null) {
            destinations.push({
                "id": "Azerbaijan".toLowerCase(),
                "title": "Azerbaijan",
                "gorunecekIsim": "Azerbaycan",
                "latitude": data.find((element) => { return element.name == "Azerbaijan" }).latitude,
                "longitude": data.find((element) => { return element.name == "Azerbaijan" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True"
            })
        }


        obj.urunler = ['Böğürtlen', 'Frenk Üzümü', 'Ahududu', 'Kuşkonmaz', 'Hindiba', 'Yaban Mersini', 'Çilek'];
        urunler.push(obj);


        var obj = {};


        obj.title = "France";
        obj.id = "France".toLowerCase();
        if (destinations.find((element) => { return element.title == "France" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "France" }), 1)
        }
        if (data.find((element) => { return element.name == "France" }) != undefined) {
            originCtx.push({
                "id": "France".toLowerCase(),
                "title": "France",
                "gorunecekIsim": "Fransa",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "France" }).latitude,
                "longitude": data.find((element) => { return element.name == "France" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("France  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Elma'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Morocco";
        obj.id = "Morocco".toLowerCase();
        if (destinations.find((element) => { return element.title == "Morocco" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Morocco" }), 1)
        }
        if (data.find((element) => { return element.name == "Morocco" }) != undefined) {
            originCtx.push({
                "id": "Morocco".toLowerCase(),
                "title": "Morocco",
                "gorunecekIsim": "Fas",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "Morocco" }).latitude,
                "longitude": data.find((element) => { return element.name == "Morocco" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Morocco  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Yaban Mersini', 'Ahududu', 'Karambola', 'Yaban Mersini (By Air)', 'Ahududu (By Air)'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Namibia";
        obj.id = "Namibia".toLowerCase();
        if (destinations.find((element) => { return element.title == "Namibia" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Namibia" }), 1)
        }
        if (data.find((element) => { return element.name == "Namibia" }) != undefined) {
            originCtx.push({
                "id": "Namibia".toLowerCase(),
                "title": "Namibia",
                "gorunecekIsim": "Namibya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Namibia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Namibia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Namibia  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Üzüm'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Senegal";
        obj.id = "Senegal".toLowerCase();
        if (destinations.find((element) => { return element.title == "Senegal" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Senegal" }), 1)
        }
        if (data.find((element) => { return element.name == "Senegal" }) != undefined) {
            originCtx.push({
                "id": "Senegal".toLowerCase(),
                "title": "Senegal",
                "gorunecekIsim": "Senegal",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Senegal" }).latitude,
                "longitude": data.find((element) => { return element.name == "Senegal" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Senegal  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Mango'];
        urunler.push(obj);


        var obj = {};


        obj.title = "South Africa";
        obj.id = "South Africa".toLowerCase();
        if (destinations.find((element) => { return element.title == "South Africa" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "South Africa" }), 1)
        }
        if (data.find((element) => { return element.name == "South Africa" }) != undefined) {
            originCtx.push({
                "id": "South Africa".toLowerCase(),
                "title": "South Africa",
                "gorunecekIsim": "Güney Afrika",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "South Africa" }).latitude,
                "longitude": data.find((element) => { return element.name == "South Africa" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("South Africa  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Kumkuat', 'Liçi', 'Liçi (By Air)', 'Elma', 'Üzüm', 'Nar', 'Avokado', 'Erik', 'Nektarin', 'Şeftali', 'Kayısı', 'Greyfurt'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Zimbabwe";
        obj.id = "Zimbabwe".toLowerCase();
        if (destinations.find((element) => { return element.title == "Zimbabwe" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Zimbabwe" }), 1)
        }
        if (data.find((element) => { return element.name == "Zimbabwe" }) != undefined) {
            originCtx.push({
                "id": "Zimbabwe".toLowerCase(),
                "title": "Zimbabwe",
                "gorunecekIsim": "Zimbabve",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Zimbabwe" }).latitude,
                "longitude": data.find((element) => { return element.name == "Zimbabwe" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Zimbabwe  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Çarkıfelek'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Madagascar";
        obj.id = "Madagascar".toLowerCase();
        if (destinations.find((element) => { return element.title == "Madagascar" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Madagascar" }), 1)
        }
        if (data.find((element) => { return element.name == "Madagascar" }) != undefined) {
            originCtx.push({
                "id": "Madagascar".toLowerCase(),
                "title": "Madagascar",
                "gorunecekIsim": "Madagaskar",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Madagascar" }).latitude,
                "longitude": data.find((element) => { return element.name == "Madagascar" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Madagascar  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Liçi', 'Liçi (By Air)'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Tanzania";
        obj.id = "Tanzania".toLowerCase();
        if (destinations.find((element) => { return element.title == "Tanzania" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Tanzania" }), 1)
        }
        if (data.find((element) => { return element.name == "Tanzania" }) != undefined) {
            originCtx.push({
                "id": "Tanzania".toLowerCase(),
                "title": "Tanzania",
                "gorunecekIsim": "Tanzanya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Tanzania" }).latitude,
                "longitude": data.find((element) => { return element.name == "Tanzania" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Tanzania  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Ahududu', 'Avokado'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Mauritius";
        obj.id = "Mauritius".toLowerCase();
        if (destinations.find((element) => { return element.title == "Mauritius" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Mauritius" }), 1)
        }
        if (data.find((element) => { return element.name == "Mauritius" }) != undefined) {
            originCtx.push({
                "id": "Mauritius".toLowerCase(),
                "title": "Mauritius",
                "gorunecekIsim": "Mauritius",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Mauritius" }).latitude,
                "longitude": data.find((element) => { return element.name == "Mauritius" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Mauritius  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Liçi', 'Liçi (By Air)', 'Bebek Ananas'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Kenya";
        obj.id = "Kenya".toLowerCase();
        if (destinations.find((element) => { return element.title == "Kenya" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Kenya" }), 1)
        }
        if (data.find((element) => { return element.name == "Kenya" }) != undefined) {
            originCtx.push({
                "id": "Kenya".toLowerCase(),
                "title": "Kenya",
                "gorunecekIsim": "Kenya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Kenya" }).latitude,
                "longitude": data.find((element) => { return element.name == "Kenya" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Kenya  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Avokado'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Thailand";
        obj.id = "Thailand".toLowerCase();
        if (destinations.find((element) => { return element.title == "Thailand" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Thailand" }), 1)
        }
        if (data.find((element) => { return element.name == "Thailand" }) != undefined) {
            originCtx.push({
                "id": "Thailand".toLowerCase(),
                "title": "Thailand",
                "gorunecekIsim": "Tayland",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "Thailand" }).latitude,
                "longitude": data.find((element) => { return element.name == "Thailand" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Thailand  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Mango', 'Mangosten', 'Mangosten (By Air)', 'Rambutan', 'Rambutan (By Air)', 'Hindistan Cevizi', 'Tamarind', 'Taze Zencefil', 'Taze Zerdeçal', 'Çarkıfelek', 'Liçi', 'Liçi (By Air)', 'Pitahaya (By Air)', 'Karambola (By Air)', 'Jackfruit (By Air)'];
        urunler.push(obj);


        var obj = {};


        obj.title = "China";
        obj.id = "China".toLowerCase();
        if (destinations.find((element) => { return element.title == "China" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "China" }), 1)
        }
        if (data.find((element) => { return element.name == "China" }) != undefined) {
            originCtx.push({
                "id": "China".toLowerCase(),
                "title": "China",
                "gorunecekIsim": "Çin",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "China" }).latitude,
                "longitude": data.find((element) => { return element.name == "China" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("China  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Sarımsak', 'Taze Zencefil', 'Pomelo'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Sri Lanka";
        obj.id = "Sri Lanka".toLowerCase();
        if (destinations.find((element) => { return element.title == "Sri Lanka" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Sri Lanka" }), 1)
        }
        if (data.find((element) => { return element.name == "Sri Lanka" }) != undefined) {
            originCtx.push({
                "id": "Sri Lanka".toLowerCase(),
                "title": "Sri Lanka",
                "gorunecekIsim": "Sri Lanka",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Sri Lanka" }).latitude,
                "longitude": data.find((element) => { return element.name == "Sri Lanka" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Sri Lanka  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Hindistan Cevizi'];
        urunler.push(obj);


        var obj = {};


        obj.title = "India";
        obj.id = "India".toLowerCase();
        if (destinations.find((element) => { return element.title == "India" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "India" }), 1)
        }
        if (data.find((element) => { return element.name == "India" }) != undefined) {
            originCtx.push({
                "id": "India".toLowerCase(),
                "title": "India",
                "gorunecekIsim": "Hindistan",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "India" }).latitude,
                "longitude": data.find((element) => { return element.name == "India" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("India  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Hindistan Cevizi', 'Üzüm'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Vietnam";
        obj.id = "Vietnam".toLowerCase();
        if (destinations.find((element) => { return element.title == "Vietnam" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Vietnam" }), 1)
        }
        if (data.find((element) => { return element.name == "Vietnam" }) != undefined) {
            originCtx.push({
                "id": "Vietnam".toLowerCase(),
                "title": "Vietnam",
                "gorunecekIsim": "Vietnam",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Vietnam" }).latitude,
                "longitude": data.find((element) => { return element.name == "Vietnam" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Vietnam  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Pitahaya', 'Pitahaya (By Air)', 'Rambutan', 'Rambutan (By Air)', 'Guanabana', 'Pomelo', 'Çarkıfelek', 'Longan', 'Liçi', 'Liçi (By Air)', 'Mangosten (By Air)', 'Karambola (By Air)', 'Jackfruit (By Air)', 'Hindistan Cevizi'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Malaysia";
        obj.id = "Malaysia".toLowerCase();
        if (destinations.find((element) => { return element.title == "Malaysia" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Malaysia" }), 1)
        }
        if (data.find((element) => { return element.name == "Malaysia" }) != undefined) {
            originCtx.push({
                "id": "Malaysia".toLowerCase(),
                "title": "Malaysia",
                "gorunecekIsim": "Malezya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Malaysia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Malaysia" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Malaysia  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Karambola', 'Ananas', 'Rambutan', 'Mangosten', 'Pitahaya', 'Pitahaya (By Air)', 'Mangosten (By Air)', 'Rambutan (By Air)', 'Karambola (By Air)', 'Jackfruit (By Air)'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Indonesia";
        obj.id = "Indonesia".toLowerCase();
        if (destinations.find((element) => { return element.title == "Indonesia" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Indonesia" }), 1)
        }
        if (data.find((element) => { return element.name == "Indonesia" }) != undefined) {
            originCtx.push({
                "id": "Indonesia".toLowerCase(),
                "title": "Indonesia",
                "gorunecekIsim": "Endonezya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Indonesia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Indonesia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Indonesia  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Mangosten', 'Pitahaya', 'Rambutan', 'Salak'];
        urunler.push(obj);


        var obj = {};


        obj.title = "New Zealand";
        obj.id = "New Zealand".toLowerCase();
        if (destinations.find((element) => { return element.title == "New Zealand" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "New Zealand" }), 1)
        }
        if (data.find((element) => { return element.name == "New Zealand" }) != undefined) {
            originCtx.push({
                "id": "New Zealand".toLowerCase(),
                "title": "New Zealand",
                "gorunecekIsim": "Yeni Zelanda",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "New Zealand" }).latitude,
                "longitude": data.find((element) => { return element.name == "New Zealand" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("New Zealand  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Kivi'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Liberia";
        obj.id = "Liberia".toLowerCase();
        if (destinations.find((element) => { return element.title == "Liberia" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Liberia" }), 1)
        }
        if (data.find((element) => { return element.name == "Liberia" }) != undefined) {
            originCtx.push({
                "id": "Liberia".toLowerCase(),
                "title": "Liberia",
                "gorunecekIsim": "Fildişi Sahili",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Liberia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Liberia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Liberia  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Hindistan Cevizi', 'Mango'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Guatemala";
        obj.id = "Guatemala".toLowerCase();
        if (destinations.find((element) => { return element.title == "Guatemala" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Guatemala" }), 1)
        }
        if (data.find((element) => { return element.name == "Guatemala" }) != undefined) {
            originCtx.push({
                "id": "Guatemala".toLowerCase(),
                "title": "Guatemala",
                "gorunecekIsim": "Guatemala",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Guatemala" }).latitude,
                "longitude": data.find((element) => { return element.name == "Guatemala" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Guatemala  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Tatlı Patates', 'Rambutan'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Poland";
        obj.id = "Poland".toLowerCase();
        if (destinations.find((element) => { return element.title == "Poland" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Poland" }), 1)
        }
        if (data.find((element) => { return element.name == "Poland" }) != undefined) {
            originCtx.push({
                "id": "Poland".toLowerCase(),
                "title": "Poland",
                "gorunecekIsim": "Polonya",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Poland" }).latitude,
                "longitude": data.find((element) => { return element.name == "Poland" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Poland  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Yaban Mersini'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Ghana";
        obj.id = "Ghana".toLowerCase();
        if (destinations.find((element) => { return element.title == "Ghana" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Ghana" }), 1)
        }
        if (data.find((element) => { return element.name == "Ghana" }) != undefined) {
            originCtx.push({
                "id": "Ghana".toLowerCase(),
                "title": "Ghana",
                "gorunecekIsim": "Gana",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Ghana" }).latitude,
                "longitude": data.find((element) => { return element.name == "Ghana" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Ghana  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Hindistan Cevizi'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Belgium";
        obj.id = "Belgium".toLowerCase();
        if (destinations.find((element) => { return element.title == "Belgium" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Belgium" }), 1)
        }
        if (data.find((element) => { return element.name == "Belgium" }) != undefined) {
            originCtx.push({
                "id": "Belgium".toLowerCase(),
                "title": "Belgium",
                "gorunecekIsim": "Belçika",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Belgium" }).latitude,
                "longitude": data.find((element) => { return element.name == "Belgium" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Belgium  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Ahududu', 'Armut', 'Böğürtlen', 'Kırmızı Elma'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Georgia";
        obj.id = "Georgia".toLowerCase();
        if (destinations.find((element) => { return element.title == "Georgia" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Georgia" }), 1)
        }
        if (data.find((element) => { return element.name == "Georgia" }) != undefined) {
            originCtx.push({
                "id": "Georgia".toLowerCase(),
                "title": "Georgia",
                "gorunecekIsim": "Gürcistan",
                "destinations": ["Türkiye"],
                "latitude": data.find((element) => { return element.name == "Georgia" }).latitude,
                "longitude": data.find((element) => { return element.name == "Georgia" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'False' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Georgia  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Sarımsak'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Egypt";
        obj.id = "Egypt".toLowerCase();
        if (destinations.find((element) => { return element.title == "Egypt" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Egypt" }), 1)
        }
        if (data.find((element) => { return element.name == "Egypt" }) != undefined) {
            originCtx.push({
                "id": "Egypt".toLowerCase(),
                "title": "Egypt",
                "gorunecekIsim": "Mısır",
                "destinations": ["Türkiye", "Hollanda"],
                "latitude": data.find((element) => { return element.name == "Egypt" }).latitude,
                "longitude": data.find((element) => { return element.name == "Egypt" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Egypt  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Iceberg Marul', 'Tatlı Patates', 'Soğan'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Swaziland";
        obj.id = "Swaziland".toLowerCase();
        if (destinations.find((element) => { return element.title == "Swaziland" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Swaziland" }), 1)
        }
        if (data.find((element) => { return element.name == "Swaziland" }) != undefined) {
            originCtx.push({
                "id": "Swaziland".toLowerCase(),
                "title": "Swaziland",
                "gorunecekIsim": "Esvatini",
                "destinations": ["Hollanda"],
                "latitude": data.find((element) => { return element.name == "Swaziland" }).latitude,
                "longitude": data.find((element) => { return element.name == "Swaziland" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Swaziland  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Netherlands" }) == null && originCtx.find((element) => { return element.title == "Netherlands" }) == null) {
            destinations.push({
                "id": "Netherlands".toLowerCase(),
                "title": "Netherlands",
                "gorunecekIsim": "Hollanda",
                "latitude": data.find((element) => { return element.name == "Netherlands" }).latitude,
                "longitude": data.find((element) => { return element.name == "Netherlands" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Avokado'];
        urunler.push(obj);


        var obj = {};


        obj.title = "Uzbekistan";
        obj.id = "Uzbekistan".toLowerCase();
        if (destinations.find((element) => { return element.title == "Uzbekistan" }) != null) {


            destinations.splice(destinations.findIndex((element) => { return element.title == "Uzbekistan" }), 1)
        }
        if (data.find((element) => { return element.name == "Uzbekistan" }) != undefined) {
            originCtx.push({
                "id": "Uzbekistan".toLowerCase(),
                "title": "Uzbekistan",
                "gorunecekIsim": "Özbekistan",
                "destinations": ["Türkiye"],
                "latitude": data.find((element) => { return element.name == "Uzbekistan" }).latitude,
                "longitude": data.find((element) => { return element.name == "Uzbekistan" }).longitude,
                "merkez": 'False' == "True",
                "alwaysShow": 'True' == "True",
                "scale": 1,
                "zoomLevel": 1,
                "zoomLongitude": 23,
                "zoomLatitude": 49.1712
            })
        } else {
            console.error("Uzbekistan  hata veriyor.");
        }


        if (destinations.find((element) => { return element.title == "Turkey" }) == null && originCtx.find((element) => { return element.title == "Turkey" }) == null) {
            destinations.push({
                "id": "Turkey".toLowerCase(),
                "title": "Turkey",
                "gorunecekIsim": "Türkiye",
                "latitude": data.find((element) => { return element.name == "Turkey" }).latitude,
                "longitude": data.find((element) => { return element.name == "Turkey" }).longitude,
                "merkez": 'True' == "True",
                "alwaysShow": 'True' == "True"
            })
        }


        obj.urunler = ['Soğan'];
        urunler.push(obj);

        // console.log("origin",originCtx);
        // console.log("dest",destinations);
        // console.log("urunler",urunler);

        $(document).ready(() => {
            HaritayiYukle(destinations, originCtx, urunler);

            setTimeout(function () {
                $("#chartdiv").height($("#chartdiv").height() + 1);
            }, 20);
        });

    </script>
    <style>
        g.amcharts-Sprite-group.amcharts-Container-group.amcharts-AmChartsLogo-group {
            display: none;
        }
    </style>
}